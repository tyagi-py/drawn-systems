{
  "name": "newsletter-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute newsletter --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute newsletter --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^6.0.3",
    "wrangler": "^4"
  }
}
