# Copy to .env.local for local development.
# URL of the deployed newsletter-subscribe Cloudflare Worker (worker/).
NEXT_PUBLIC_SUBSCRIBE_ENDPOINT=https://newsletter-subscribe.<your-subdomain>.workers.dev
